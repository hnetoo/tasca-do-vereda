[build]
# Configurações de build otimizadas para Windows MSI

# Minimizar binário
strip = true
opt-level = 3
lto = true
codegen-units = 1

# Desabilitar símbolos de debug em release
[profile.release]
strip = true
opt-level = 3
lto = true
codegen-units = 1
